#define _CRT_SECURE_NO_WARNINGS 1
#include <stdio.h>
int main() {
	int x = 1;
	int y = 1;
	while (1) {
		scanf("%d%d",  & x, &y);
		if (x > y) {
			printf("%d>%d/n", x, y);
		}
		else if (x = y) {
			printf("%d=%d/n", x, y);
		}
		else {
			printf("%d<%d/n", x, y);
		}
	};
	return 0;
}
